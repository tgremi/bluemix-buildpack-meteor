#!/usr/bin/env bash
# bin/release <build-dir>

cat <<EOF
---
addons:
default_process_types:
  web: .meteor/meteor_build/app/bundle/node_modules/.bin/launcher -- .meteor/meteor_build/bin/node .meteor/meteor_build/app/bundle/main.js
EOF
